version: '3.8'

services:
  backend:
    build: .
    ports:
      - "80:80"
    volumes:
      - .:/app
    env_file:
      - .env
    command: ["uvicorn", "backend.app.main:app", "--host", "0.0.0.0", "--port", "80", "--reload"]